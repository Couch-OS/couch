name: Integration admission

on:
  # Keep the final admission job present on every pull request. If branch
  # protection later requires it, unrelated pull requests will finish green
  # instead of waiting forever on a path-filtered workflow that never existed.
  pull_request:
  push:
    branches: [dev, main]
    paths:
      - 'clients/**'
      - 'daemon/**'
      - 'model/**'
      - 'web/**'
      - 'web/**'
      - 'integrations/**'
      - 'tools/integrations/**'
      - 'tools/tests/integrations-e2e.py'
      - 'tools/tests/config-crossload.*'
      - 'tools/tests/old-package-wire.sh'
      - '.github/workflows/integration-admission.yml'
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: integration-admission-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  changes:
    runs-on: ubuntu-24.04
    outputs:
      integration: ${{ steps.scope.outputs.integration }}
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - name: Select integration work
        id: scope
        env:
          EVENT_NAME: ${{ github.event_name }}
          BASE_SHA: ${{ github.event.pull_request.base.sha }}
        run: |
          changed=true
          if [ "$EVENT_NAME" = pull_request ] && git diff --quiet "$BASE_SHA"...HEAD -- \
            clients daemon model web integrations tools/integrations \
            tools/tests/integrations-e2e.py tools/tests/config-crossload.sh \
            tools/tests/config-crossload.rs tools/tests/old-package-wire.sh \
            .github/workflows/integration-admission.yml; then
              changed=false
          fi
          echo "integration=$changed" >> "$GITHUB_OUTPUT"

  catalog:
    needs: changes
    runs-on: ubuntu-24.04
    timeout-minutes: 25
    steps:
      - uses: actions/checkout@v6
      - name: Validate catalog policy
        run: |
          python3 tools/integrations/validate_catalog.py
          python3 -m unittest tools.integrations.test_validate_catalog -v
      - uses: dtolnay/rust-toolchain@stable
        if: needs.changes.outputs.integration == 'true'
      - uses: Swatinem/rust-cache@v2
        if: needs.changes.outputs.integration == 'true'
        with:
          workspaces: clients
      - name: Run shared host protocol tests
        if: needs.changes.outputs.integration == 'true'
        run: cargo test --locked --manifest-path clients/Cargo.toml -p couch-plugin --test protocol
      - name: Run every required package case
        if: needs.changes.outputs.integration == 'true'
        run: |
          python3 tools/integrations/validate_catalog.py --run-tests
          cargo test --locked --manifest-path clients/Cargo.toml -p couch-sonos --test admission concurrent_package_startup_is_offline_and_race_free -- --exact

  product-flow:
    needs: changes
    if: needs.changes.outputs.integration == 'true'
    runs-on: ubuntu-24.04
    timeout-minutes: 25
    steps:
      - uses: actions/checkout@v6
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
        with:
          workspaces: |
            clients
            daemon
      - name: Build host binaries
        run: |
          cargo build --locked --manifest-path clients/Cargo.toml -p couch-echo --bin couch-plugin-echo
          cargo build --locked --manifest-path daemon/Cargo.toml -p couch-confd
      - name: Test package lifecycle library
        run: cargo test --locked --manifest-path daemon/Cargo.toml -p couch-integrations
      - name: Test legacy connection conversion, config persistence and shared device connections
        run: |
          cargo test --locked --manifest-path model/Cargo.toml -p couch-model
          cargo test --locked --manifest-path clients/Cargo.toml -p couch-control
          cargo test --locked --manifest-path daemon/Cargo.toml -p couch-confd
      # The last release's model source, built beside this one: it has to start
      # on every file this tree saves, and this tree must not move a byte of
      # the files it saved. Fetches the release tag itself.
      - name: Exchange saved configurations with the last release
        run: tools/tests/config-crossload.sh
      # The packages already in the feed were built from an older couch-plugin
      # whose every wire type refuses unknown fields. Build echo and Sonos from
      # that source and run this tree's admission and subprocess suites against
      # those executables. This tree's side reuses clients/target, which this
      # job has already cached; the old side is a cold build of two small
      # crates in a temporary directory.
      - name: Drive packages built from the published SDK with this host
        env:
          COUCH_OLD_WIRE_REUSE_TARGET: 1
        run: tools/tests/old-package-wire.sh
      - name: Exercise daemon, package, HTTP, panel, and fake device together
        run: python3 tools/tests/integrations-e2e.py

  management-browser:
    needs: changes
    if: needs.changes.outputs.integration == 'true'
    runs-on: ubuntu-24.04
    timeout-minutes: 25
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-node@v6
        with:
          node-version: 24
      - uses: dtolnay/rust-toolchain@stable
        with:
          targets: wasm32-unknown-unknown
      - uses: Swatinem/rust-cache@v2
        with:
          workspaces: web
      - name: Install the pinned browser bundle builder
        run: |
          curl --fail --location --retry 3 --output "$RUNNER_TEMP/trunk.tar.gz" \
            https://github.com/trunk-rs/trunk/releases/download/v0.21.14/trunk-x86_64-unknown-linux-gnu.tar.gz
          echo "f2b4680cd239693a646a2795e4633c625328d7b2a044fbe749fa3a2fe9e7036b  $RUNNER_TEMP/trunk.tar.gz" | sha256sum --check
          mkdir -p "$RUNNER_TEMP/trunk-bin"
          tar -xzf "$RUNNER_TEMP/trunk.tar.gz" -C "$RUNNER_TEMP/trunk-bin" trunk
          echo "$RUNNER_TEMP/trunk-bin" >> "$GITHUB_PATH"
      - name: Build the package-management UI
        run: trunk build --release
        working-directory: web/couch-web
      - name: Install browser test tooling
        run: |
          npm install --prefix build/webui-review playwright@1.57.0
          build/webui-review/node_modules/.bin/playwright install --with-deps chromium
      - name: Exercise package management in Chromium
        run: |
          python3 -m http.server 18093 --bind 127.0.0.1 --directory web/couch-web/dist > "$RUNNER_TEMP/couch-web.log" 2>&1 &
          COUCH_TEST_URL=http://127.0.0.1:18093 node web/tests/integrations.mjs
          COUCH_TEST_URL=http://127.0.0.1:18093 node web/tests/plugin-components.mjs
          COUCH_TEST_URL=http://127.0.0.1:18093 node web/tests/plugin-children.mjs
      # The same bundle and the same browser, for the other screen whose copy
      # is asserted rather than its behaviour: every endpoint is mocked, so no
      # update service and no remote are involved.
      - name: Exercise the Updates screen in Chromium
        run: |
          python3 -m http.server 18095 --bind 127.0.0.1 --directory web/couch-web/dist > "$RUNNER_TEMP/couch-web-updates.log" 2>&1 &
          COUCH_TEST_URL=http://127.0.0.1:18095 node web/tests/updates.mjs
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: integration-management-browser
          path: |
            build/webui-review/integrations-mobile.png
            build/webui-review/integrations-desktop.png
            build/webui-review/plugin-components-mobile.png
            build/webui-review/plugin-components-desktop.png
            build/webui-review/plugin-components-field-error.png
            build/webui-review/plugin-children-mobile.png
            build/webui-review/plugin-children-confirm.png
            build/webui-review/plugin-children-controls.png
            build/webui-review/updates-installed-mobile.png
            build/webui-review/updates-installed-desktop.png
            ${{ runner.temp }}/couch-web.log
            ${{ runner.temp }}/couch-web-updates.log
          if-no-files-found: warn

  native-package:
    needs: changes
    if: needs.changes.outputs.integration == 'true'
    runs-on: ubuntu-24.04
    timeout-minutes: 45
    steps:
      - uses: actions/checkout@v6
      - uses: dtolnay/rust-toolchain@stable
        with:
          targets: armv7-unknown-linux-musleabihf
      - uses: Swatinem/rust-cache@v2
        with:
          workspaces: |
            clients
            daemon
      - uses: docker/setup-qemu-action@v3
        with:
          platforms: arm
      - name: Install the pinned ARM C toolchain
        run: tools/fetch-zig.sh
      - name: Cross-build package and daemon binaries
        run: |
          . tools/arm-cc-env.sh
          python3 tools/integrations/validate_catalog.py
          while IFS=$'\t' read -r package binary; do
            (cd clients && cargo build --locked --release --target armv7-unknown-linux-musleabihf -p "$package" --bin "$binary")
          done < <(jq -r '.integrations[] | [.cargo_package, .binary] | @tsv' integrations/catalog.json)
          (cd daemon && cargo build --locked --release --target armv7-unknown-linux-musleabihf -p couch-confd)
      - name: Build, sign, index, install, reject tampering, and verify ARM packages
        run: tools/integrations/smoke.sh "$RUNNER_TEMP/integration-smoke"

  admission:
    if: always()
    needs: [changes, catalog, product-flow, management-browser, native-package]
    runs-on: ubuntu-24.04
    steps:
      - name: Require every applicable admission job
        env:
          CHANGES: ${{ needs.changes.result }}
          INTEGRATION: ${{ needs.changes.outputs.integration }}
          CATALOG: ${{ needs.catalog.result }}
          PRODUCT: ${{ needs.product-flow.result }}
          BROWSER: ${{ needs.management-browser.result }}
          PACKAGE: ${{ needs.native-package.result }}
        run: |
          test "$CHANGES" = success
          test "$CATALOG" = success
          if [ "$INTEGRATION" = true ]; then
            test "$PRODUCT" = success
            test "$BROWSER" = success
            test "$PACKAGE" = success
          else
            test "$PRODUCT" = skipped
            test "$BROWSER" = skipped
            test "$PACKAGE" = skipped
          fi
